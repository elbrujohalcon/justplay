<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activities.MainActivity">

    <TextView
        android:id="@+id/exampleRaw"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:gravity="center"
        android:text="@string/example_raw" />

    <ImageButton
        android:id="@+id/playRawSound"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/exampleRaw"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:background="@color/colorPrimary"
        android:soundEffectsEnabled="false"
        android:src="@android:drawable/ic_media_play"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/exampleStream"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/playRawSound"
        android:layout_marginTop="30dp"
        android:gravity="center"
        android:text="@string/example_stream" />

    <ImageButton
        android:id="@+id/playStreamSound"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/exampleStream"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:background="@color/colorPrimary"
        android:soundEffectsEnabled="false"
        android:src="@android:drawable/ic_media_play"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/exampleUri"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/playStreamSound"
        android:layout_marginTop="30dp"
        android:gravity="center"
        android:text="@string/example_URI" />

    <ImageButton
        android:id="@+id/playUriSound"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/exampleUri"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:background="@color/colorPrimary"
        android:soundEffectsEnabled="false"
        android:src="@android:drawable/ic_media_play"
        tools:ignore="ContentDescription" />
</RelativeLayout>
